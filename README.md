# Tabla de Multiplicación

App de consola que muestra la tabla de multiplicacion

```
     --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Base de la tabla de multiplicar             [number] [required]
  -l, --list     Permite listar en pantalla los resultados
                                                      [boolean] [default: false]
  -h, --hasta    Permite saber hasta que numero vamos a multiplicar la base
                                                          [number] [default: 10]
```